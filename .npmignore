# Source (compiled output is in dist/)
src/

# Tests
tests/

# Internal docs
docs/

# Dev config
tsconfig.json
vitest.config.ts
vitest.e2e.config.ts
vitest.integration.config.ts

# Claude Code config (dev-only)
# NOTE: The package.json "files" field is the sole authority for what ships.
# .npmignore is IGNORED when "files" is present. To include new assets,
# add them to package.json "files" — NOT here.
CLAUDE.md

# Landing page site
site/

# Image assets (referenced via GitHub, not needed in package)
assets/

# CI
.github/

# Source maps (reduces package size)
dist/**/*.js.map
dist/**/*.d.ts.map
