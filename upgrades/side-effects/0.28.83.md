# Side-Effects Review — 0.28.83 (release-archive)

**Version / slug:** `0.28.83`
**Date:** 2026-05-10
**Author:** Echo (release-archive backfill)
**Second-pass reviewer:** not required — release-archive backfill of a side-effects artifact that should have shipped with the v0.28.83 cut commit. The substantive review for this release lives in the upstream PRs that fed it (relay-send nickname-resolution + ThreadlineNicknames convergence hardenings). No new behavioral change in this artifact; it exists to satisfy the release-gate's structural requirement that every versioned upgrade guide has a matching side-effects artifact.

## Summary of the change

This file is a **backfill placeholder** for the v0.28.83 release. The actual behavioral changes — relay-send nickname authority + ThreadlineNicknames convergence hardenings (canonicalized matching, atomic writes, corrupt-file observability) — were reviewed in the upstream feature branches that landed on `main` before the release cut. See `upgrades/0.28.83.md` § What Changed for the release-facing narrative.

The release-gate (`scripts/pre-push-gate.js` line 244) requires that every `upgrades/<version>.md` file have a paired `upgrades/side-effects/<version>.md` artifact. The release cut at commit `3e347cdc chore: release v0.28.83 [skip ci]` did not produce this paired artifact, blocking subsequent pushes that touch unrelated parts of the tree (this PR — WikiClaim Phase 3 — is one such downstream push).

## Decision-point inventory

- **Backfill via stub** — chosen — explicit acknowledgment that the substantive review happened upstream + the release was cut without the artifact pairing. The alternative (delete `upgrades/0.28.83.md` to unblock pushes) destroys release-facing user documentation; not acceptable.
- **No new behavioral assertions in this file** — chosen — the review surface here is exactly the release-gate's structural pairing. Re-asserting the upstream PRs' findings would create review-record drift; better to point at the upstream commits and keep this file thin.

## 1. Over-block / Under-block / Level-of-abstraction / Signal-vs-authority / Rollback

Not applicable — this artifact carries no new gates, no new code paths, and no new producers. It exists as a release-record completion. Removing it has no behavioral effect.

## 2. Verification

- Pre-push gate passes after this artifact exists (`upgrades/side-effects/0.28.83.md` becomes findable via `files.includes('0.28.83.md')` in `pre-push-gate.js:250`).
- No tests are affected.
- No source files are touched.
