# Convergence Round 1 — Self-Unblock Before Escalating (NOT yet converged)

Ran 2026-06-14 ~12:40Z. External: gemini-2.5-pro (CLEAN) + codex gpt-5.5 (6 findings). Internal: security, adversarial, integration, decision-completeness, lessons-aware, scalability. Conformance gate: degraded (server error, non-authoritative). **Material findings remain → spec requires Phase-2 rework before it can converge or build.**

## DOMINANT MATERIAL FINDING (integration + lessons-aware, independently)
**The spec FORKS a duplicate of the existing `src/monitoring/BlockerLedger.ts` instead of EXTENDING it.** BlockerLedger already implements the core mechanism MORE rigorously: `settleTrueBlocker()` MANDATES a recorded FAILED self-fetch attempt before a credential/account blocker can be filed operator-required (`SELF_FETCH_KINDS` + `missing_failed_attempt`) — that IS "exhaust before escalating, mechanically", and it's already a HARD reject routed through a real Tier-1 LLM authority (B17 `settleAuthority`), satisfying Signal-vs-Authority. It has a closed `TRUE_BLOCKER_KINDS` taxonomy, temporal access-request-after-attempt proof, audit at `logs/blocker-decisions.jsonl`, and the `<blocker-ledger-data>` untrusted-data envelope. The spec's `selfUnblockAttempts[]`/`ladderRung`/`escalationReason` + a new `evaluateSelfUnblock` gate + a separate `logs/self-unblock-ledger.jsonl` + `GET /self-unblock/ledger` largely re-skin `BlockerEntry.failedAttempt`/`reasonKind`/`rebuttal`. **REWORK: rewrite §5 as a genuine BlockerLedger extension** (map the ladder onto the existing pipeline/taxonomy; reuse blocker-decisions.jsonl + the /blockers routes + the envelope; make SelfUnblockChecklist the by-product feeder for the already-required failedAttempt evidence). Nest config under `monitoring.blockerLedger.*` (one gate), not a parallel `selfUnblock.*`.

## OTHER MATERIAL FINDINGS
- DEV-GATE (integration): §6 hardcodes `enabled:false` — that resolves DARK even on dev. The dev-gate only fires when `enabled` is OMITTED (`resolveDevAgentGate = explicitEnabled ?? !!developmentAgent`). FIX: omit `enabled` (precedent: `blockerLedger: {}`). Migration: applyDefaults backfills the block automatically; the ONLY migration needed is an `enabled:false`-strip for existing agents (mirror `migrateConfigCredentialRepointingDevGate`). Register in `DEV_GATED_FEATURES` with a justification or `tests/unit/devGatedFeatures-wiring.test.ts` fails.
- PROVENANCE (adversarial + security + codex): `selfUnblockAttempts` is self-asserted + gameable (hand-author the list; or set `ladderRung:0` to skip the gate entirely; or stuff one junk token). FIX: gate accepts only a checklist-PRODUCED, provenance-stamped result object (run id + per-probe `{source,reachable,holdsRelevantCred,probedAt}`), not a free-form string array; cross-check ladderRung against the structured probe results.
- SESSION SECURITY (security + codex): §5.5 "durable" must be IN-MEMORY-ONLY (or keychain) — never on disk/log/config/argv (BW_SESSION is a vault-decryption credential). Add a test asserting it never appears in the ledger or argv. Specify TTL/revocation/least-privilege.
- AUDIT SCRUB (security): route every ledger field through the existing credential-scrub (`CredentialAuditEmit`/`AuditTrail`) before append; a probe records only `holdsRelevantCred:boolean`, never the value.
- ROUTE AUTH (security): `GET /self-unblock/ledger` must be Bearer-gated, NEVER added to the auth-exempt allowlist, 503-when-dark check AFTER auth (unauthenticated → 401 not 503), `Cache-Control:no-store`, served through the untrusted-data envelope.
- BOUNDARY ENFORCEMENT (adversarial): §11 is prose-only. Add a wiring-integrity test proving a self-unblock action touching an external account is still evaluated by the external-operation + mandate gates. Add a "rung floor": irreversible/cost-bearing/out-of-scope/policy-sensitive actions have a MINIMUM rung of 1 (approval) even if a credential exists — capability ≠ authority. Lead the §1/§10 template wording with the boundary, not "find a way".
- KNOW-YOUR-PRINCIPAL (lessons-aware): a rung-1 "org-authority granted me access" path must resolve against a VERIFIED principal (mandate/verified-operator), never a name in content.
- WARN→HARD (lessons-aware): the future `hard` flip is a new blocking authority — add a deferred "requires a separate review-convergence pass" note; in hard mode the judgment must route through B17/Tier-1, never field-presence alone.

## MINOR (fold in): probe per-timeout (10-15s, fail-toward-unreachable) + sequential-early-exit ordering; `GET /self-unblock/ledger` default-bounded `?limit=` + skip-corrupt-lines (precedent ReapLog.read); codex: design overfits credential-discovery (blockers aren't all credential blockers — define categories); "ban the word blocked" is brittle (assert positive shape instead).

## DECISION-COMPLETENESS: clean (all decisions frontloaded, §15 (none) honest). SCALABILITY: no material (4 minors). GEMINI: CLEAN.

## VERDICT: NOT converged. Phase-2 rework required (dominant: reframe as BlockerLedger extension). Then re-review. DO NOT tag/build until converged. This round already paid for itself — it caught a duplicate-subsystem mistake before any code was written.

## PHASE-2 REFRAME BLUEPRINT (the corrected design — execute this next)
Grounded against src/monitoring/BlockerLedger.ts. The existing ledger ALREADY IS the exhaustion gate: `settleTrueBlocker()` requires a recorded failed attempt (`AttemptEvidence`, type self-fetch for SELF_FETCH_KINDS else dry-run) before a `true-blocker` can settle, rejects `missing_failed_attempt` (HARD, not warn), proves the access-request came AFTER the attempt, routes the JUDGMENT through `SettleAuthority` (B17 Tier-1 LLM), uses the closed `TRUE_BLOCKER_KINDS` taxonomy, and audits to logs/blocker-decisions.jsonl with the `<blocker-ledger-data>` envelope.

So the standard must NOT add a parallel gate/ledger/log. Its GENUINE NEW CONTRIBUTIONS over BlockerLedger are exactly four:
1. **SelfUnblockChecklist** (the only substantial new code): a deterministic, ordered probe list (own vault → org Bitwarden → cloud accounts authed [Vercel/CF/GitHub/launchd] → MCP → browser → self-owned-resource) that SYSTEMATICALLY PRODUCES the `AttemptEvidence.failedAttempt` BlockerLedger already requires — turning "you must record a failed attempt" into "here is the standard set of sources you must have probed". Its structured per-probe result {source, reachable, holdsRelevantCred, probedAt} feeds the ledger's existing failedAttempt; provenance is automatic (the checklist stamps it) → closes the adversarial "self-asserted/gameable list" finding for free, because the ledger already won't settle without a real failedAttempt.
2. **Human-requirement LADDER (rung 0/1/2) + rung-FLOOR**: maps onto the existing `AuthorityCheckEvidence` {agentHasAuthority, userHasAuthority}; adds the explicit "aim for the lowest rung, named exactly" framing AND the rung-FLOOR (irreversible/cost-bearing/out-of-scope/policy-sensitive → minimum rung 1 even if a cred exists; capability≠authority). Rung-1 grants resolve against a VERIFIED principal (mandate/verified-operator), never content (Know-Your-Principal).
3. **Durable org-vault (Bitwarden) session**: in-memory/keychain only, never on disk/log/argv, never on the cross-machine secretSync path; machine-local; flag-gated; wires to unlock-bw.sh. (This is what lets probe #2 actually reach the org vault.)
4. **Constitutional encoding**: CLAUDE.md template section (generateClaudeMd) + migrateClaudeMd + STANDARDS-REGISTRY entry — the awareness layer, leading with the BOUNDARY ("within permissions / granted scope") not "find a way".

CONFIG: nest under `monitoring.blockerLedger.*` (one gate/posture) — e.g. `monitoring.blockerLedger.selfUnblockChecklist.enabled` + `.durableVaultSession.enabled`; OMIT `enabled` keys (dev-gate fires only when omitted: `explicitEnabled ?? !!developmentAgent`); register in DEV_GATED_FEATURES with a justification; add an `enabled:false`-strip migration for existing agents.
WARN→HARD: there is NO new warn-gate — BlockerLedger's gate is already the (hard) authority. Drop the spec's separate `evaluateSelfUnblock` warn-first gate entirely (it was the duplicate). The "measure before teeth" idea is moot: the existing gate already has teeth via the LLM authority.
ROUTE: no new `/self-unblock/ledger` — extend the existing `/blockers/*` read surface (or a thin read-only view over blocker-decisions.jsonl) — Bearer-gated, never auth-exempt, 503-after-auth, no-store, envelope.
TESTS: the new code is small (the checklist module + the ladder/rung-floor mapping + the vault-session wiring) → 3-tier tests scoped to those; PLUS a wiring-integrity test proving a self-unblock action through an external account is still evaluated by the external-operation + mandate gates (prove §11 mechanically).
NET: the reframed standard is SMALLER and CLEANER than the original spec — it leverages the existing ledger instead of duplicating it. This is the single biggest win from round 1.

## PHASE-2 REWRITE — DONE + re-verified (2026-06-14 ~13:00Z)
Executed the reframe blueprint: the spec is REWRITTEN to EXTEND BlockerLedger (new §0 "Foundation"; §5 = the four genuine additions only: SelfUnblockChecklist that feeds BlockerLedger's existing required failedAttempt; ladder+rung-floor on AuthorityCheckEvidence; in-memory-only durable vault session; constitutional encoding). Dropped the duplicate `evaluateSelfUnblock` warn-gate entirely (BlockerLedger's settle gate is already the HARD + Tier-1-authority gate). Config nested under monitoring.blockerLedger.* with `enabled` OMITTED (dev-gate). Route extends /blockers (Bearer/never-auth-exempt/503-after-auth/no-store/envelope). Added rung-FLOOR, Know-Your-Principal on rung-1, relevance hard-filter, in-memory-session test, wiring-integrity test.
RE-VERIFIED: gemini-2.5-pro re-review of the rewritten spec = "extends existing architecture rather than building redundant systems" → the DOMINANT fork-vs-extend flaw is RESOLVED. Only 2 MINOR findings (relevance-filter mechanism; timeout granularity) — both FOLDED IN (relevance = deterministic tag/zone match, not LLM; per-probe-class timeouts). Spec body hash now 1d37ab0d (codex re-pass + full 6-reviewer re-convergence round = next step before tagging).
REMAINING for CMT-1519: full re-convergence round (all 6 internal + codex + gemini on the rewritten body, confirm zero material) → ELI10 convergence report + ELI16 companion (≥800 chars, REQUIRED for the tag) → write-convergence-tag.mjs → /instar-dev build (SelfUnblockChecklist module + ladder/rung-floor mapping + vault-session wiring + /blockers-extension route + 3-tier tests + enabled-strip migration + STANDARDS-REGISTRY + CLAUDE.md template + DEV_GATED_FEATURES entry) → PR vs JKHeadley → CI → merge. All runnable INLINE (no decoupled -p needed). The convergence process already paid for itself: design went from flawed (duplicate subsystem) to sound (clean BlockerLedger extension).
