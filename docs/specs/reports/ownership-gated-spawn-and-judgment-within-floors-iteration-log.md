# Convergence iteration log — ownership-gated-spawn-and-judgment-within-floors

Worktree: /Users/justin/.instar/agents/echo/.worktrees/echo-judgment-within-floors-spec (branch echo/judgment-within-floors-spec, base upstream/main @ 7406c8e61)
Spec: docs/specs/ownership-gated-spawn-and-judgment-within-floors.md

## Round 1 — 2026-07-10 ~20:00 PDT
- Body hash at external pass: dbc0db65112f230390be77f159ae955ecb1466eed3f8df2788f077366c48e608
- Standards-Conformance Gate: ran (3 flags — Migration Parity, Testing Integrity wiring tests, Maturation Path graded-review job) — 51 standards, canary ok (51 articles), degraded:false
- Internal reviewers (6): security, scalability, adversarial, integration, decision-completeness, lessons-aware — session model claude-fable-5 (D7 disclosure: subagents inherit session model)
- External: codex-cli:gpt-5.5 status=ok verdict=MINOR ISSUES (5 findings) | gemini-cli:gemini-2.5-pro status=ok verdict=MINOR ISSUES (3 findings)
- Clean-door anthropic (claude-code): unavailable, reason=untrusted-framework (deployed dist module predates claude-code trusted-family entry; recorded honestly — NOT a cross-model pass either way, crossFamily:false)
- Framework detection recorded to .instar state (activationRecorded:true; codex-cli + gemini-cli active → externals MANDATORY per 7-day baseline)

### External findings (round 1)
codex-1 [§3.2] registry convergence semantics underspecified — consistency model under partition / stale views / simultaneous lease transitions / two reconcilers; CAS needs linearized write authority (fencing epoch) or conflict-resolution rule.
codex-2 [§3.2.1] "most recent user interaction" fallback can legitimize the duplicate (it got the latest message BECAUSE of the bug) — demote behind stronger evidence; escalate when only differentiator is duplicate-side recency.
codex-3 [§3.4/3.5] justify arbiters vs deterministic workflow policy / lease libs — add alternatives rationale.
codex-4 [§3.3] owner-dark notice vs one-voice speaker election contradiction — who may speak when owner is dark? prove no duplicate notices across adapters.
codex-5 [§7] standards ratification bundled with runtime fix — separate approval checkboxes or staged adoption.
gemini-1 [§3.1/3.2] alternatives-considered: bespoke vs etcd/ZK locks, Kafka consumer-groups.
gemini-2 [§3.2.1/J2] justify J2 LLM vs deterministic tie-breaker (lowest machine id etc.) — why is LLM superior in the ambiguous residue?
gemini-3 [§3.2] partition behavior: lease holder can't see duplicates inside an isolated partition; stale reads — state accepted behavior.

### Internal findings (round 1)
(pending — agents running)

#### security (internal, round 1) — VERDICT: FINDINGS (3 MATERIAL, 5 minor)
SEC1-1 MAT [§3.5/posture] "full body never leaves via HTTP" is FALSE as designed: dashboard file routes serve project dir by default (allowedPaths ['./'], src/server/fileRoutes.ts:21) incl. logs/; blockedFilenames only key/env shapes; PIN-gated download over tunnel → full-context JSONL HTTP-readable. liveTailRedaction is credential-shape-only (no PII scrub). → store full-body log outside servable root or never-served denylist; 0600; backup/support-bundle exclusion; state redaction honesty in §3.5.
SEC1-2 MAT [§3.4 J2/§3.2.4] tail prompt-injection can steer move-ownership-to-working-copy; floor has no mechanical precondition on that option (unlike J1 claim bar). → J2 may only PROPOSE the move; valid only with deterministic work evidence (registry-recorded live run, work-gate probe); tail text alone can never select it.
SEC1-3 MAT [§3.2.1] intended-owner trusts peer-attested poll rows ("live autonomous run", "recent user interaction"); fabricated peer claim can win ownership in the owner=? state; CAS serializes writes but doesn't validate winner inputs. → when no registry record, require corroboration beyond bare peer poll (signed heartbeat/journal), or route to J2→escalate rather than auto-converge; pin-quarantine precedent.
SEC1-4 min [§3.5] provenance HTTP surface unnamed/auth unstated; ?scope=pool fan-out → state Bearer, redact-before-wire, type/length-clamp + untrusted-data envelope for peer rows.
SEC1-5 min [§3.3/3.4] J1 per-message invocation under burst → LLM budget burn/shed pressure → cache J1 verdict per topic for hold window/cooldown, re-invoke on state change.
SEC1-6 min [§3.4 J1] J1 free-text context lacks untrusted-data envelope requirement (only J2 declared) → require envelope for BOTH arbiters.
SEC1-7 min [§3.2] reconciler doesn't defer on open stale-owner episode/active J1 hold; record flips don't count toward breaker → defer + count flips.
SEC1-8 min [§3.1] error→spawn-locally arm as standing bypass via induced registry read errors → track error-verdict frequency, escalate/breaker on N consecutive.

#### scalability (internal, round 1) — VERDICT: FINDINGS (4 MATERIAL, 4 minor)
SC1-1 MAT [§3.2.1] "piggyback the per-peer poll" is request-driven (runs inside GET /sessions?scope=pool handler, routes.ts:7468-7532, only when dashboard polls) — reconciler never runs unattended exactly when duplicates form (overnight). → explicit tick cadence riding WS4.4(f) shared pool poll-cache (TTL+load-shed built), per-tick reconcile cap (precedent maxReapsPerPass, maxFailoverReleasesPerTick:5).
SC1-2 MAT [§3.3/3.4] J1 granularity unspecified: burst N msgs × M topics of dark owner → N×M arbiter calls exactly when providers degraded + spawn-cap contended (arbiters ride buildIntelligenceProvider, compete with gating calls for 8 host slots); no timeout budget. → pin J1 per-(owner-machine, outage-episode) cached/TTL shared across topics+messages; explicit timeout (≤5s like swapAttemptTimeoutMs); non-gating shed→deterministic default.
SC1-3 MAT [§3.3 rung 3] dark-queue "queued and answered when it returns" implies unbounded message-body retention (live queue's caps exist for this: maxPerSession:50, maxTotal:500, maxPayloadBytes:65536, maxHeldTotal:150, ConfigDefaults.ts:1367+); hold state unbounded when queue dark. → bound dark-queue retention (small in-memory cap + honest overflow notice) OR make rung 3 notice-only (user resends); cap concurrent holds.
SC1-4 MAT [§3.5/§4] Increment-1 provenance rows for EVERY deterministic verdict, J2 rows embed BOTH tails — row size unbounded; write discipline unspecified on inbound path (sync I/O main-thread incident history). → async buffered appends, per-row byte clamp (tail-slice cap), sampling knob for deterministic rows during soak.
SC1-5 min [§3.1] resolveOwnershipSafe pins to in-memory Map today (SessionOwnershipRegistry.ts:46) but store interface designed for future git-durable substrate with I/O reads. → pin seam to in-memory/cached view with accepted staleness; never sync durable read on inbound path.
SC1-6 min [§3.2.5] breaker counters no TTL prune → drop idle >24h.
SC1-7 min [§3.3 rung 3] burst → N notices; relay dedup is length-gated (short notices pass) → per-(topic, outage-episode) notice dedup at the ladder.
SC1-8 min [§3.2] mass-duplicate events (split-brain heal): CAS convergence writes + pin repairs + J2 calls unbounded per tick → per-tick convergence cap.

#### decision-completeness (internal, round 1) — VERDICT: FINDINGS (4 MATERIAL, 6 minor); COUNTS: frontloaded=7 cheap-tags=3 contested-cleared=2 contested-rejected=1
DC1-1 MAT [§3.5/FD#6] cheap tag REJECTED on "redaction depth are config" — decision table says redaction is INVARIANT; under-redacted rows during soak are durable disclosures. → retention stays cheap; redaction contract pinned fixed invariant (scrubString + {redacted,full} split, non-configurable).
DC1-2 MAT [§3.3] silence hard ceiling has NO VALUE; holdMaxMs is 90s vs incident's ~34min window — new threshold builder must invent. → frontload concrete ceiling (e.g. 10 min) + named config key, cheap behind Increment-1 dryRun.
DC1-3 MAT [§3.3 rung 3] queue-dark branch mechanism undecided; "your message is queued" FALSE in that state; "retry on owner return" names no store. → pin queue-dark floor to state-accurate resend-only notice (G1 precedent) or name the retry source; per-state wording.
DC1-4 MAT [§3.4] arbiter routing-registry values unstated: COMPONENT_CATEGORY, nature A/B, LLM_UNTRUSTED_INPUT + FD5b injection-exposure (J2 reads tails → gates injection-unsafe doors), bench floor tier. → frontload assignments (both sentinel, nature B; J2 untrusted:true/injection-exposed; named floor tier) or cheap behind Increment-3 shadow with starting values.
DC1-5 min [§0/§7] §7 text names guards that don't exist yet (SpawnAdmission.ts etc.) → registry text lands in SAME PR as Increment 1 guard files.
DC1-6 min [§4/FD#5] "three flags" names only two keys; arbiter flag key, provenance config keys, HTTP route path, job name unnamed → name them.
DC1-7 min [§3.6] classification gate lands enforcing with Increment 1; no grandfathering for mid-convergence specs; no verbatim question text → pin grandfathering + final SKILL.md wording.
DC1-8 min [§3.4/OQ] J2 tail-slice size is author-answerable, not a user decision → decide default (live-tail chokepoint bound), move to Frontloaded.
DC1-9 min [§3.3] "mid-restart" wording doesn't fit all other-dark causes → one honest cause-generic phrasing or per-class table.
DC1-10 min [§3.1] error-arm dedupe key + attention wording unstated → state key (per-topic per-episode) + draft wording, or explicit cheap tag.

#### integration (internal, round 1) — VERDICT: FINDINGS (4 MATERIAL, 6 minor)
INT1-1 MAT [§3.3] ladder floor depends on dark machinery: holdVerdict constructed only inside inbound-queue block (server.ts:21255-21270, dryRun gates construction); rung-3 custody = same dark queue → fleet ladder collapses to notice-only while notice text claims "queued" (false) + "retry on owner return" has no mechanism/state. → rollout-stage behavior matrix (queue dark/dryRun/live, hold trailing one stage), per-stage honest wording ("resend when it's back" when custody dark), name retry mechanism + durability or bound the loss window.
INT1-2 MAT [posture notices row] SpeakerElection.decideInner rule 1 returns speak=false (owner-other) with NO owner-liveness input (SpeakerElection.ts:122-127 — the incident's own log line): voice-holder IS the dark owner → the never-silence notice is structurally silenced. → owner-dark carve-out in election (dark owner ⇒ notifier speaks once) OR notice rides deterministic G1 path bypassing election, with dedupe vs owner recovering mid-flight.
INT1-3 MAT [§3.6] migration parity: SKILL.md + side-effects template are agent-installed, installBuiltinSkills never overwrites; exact precedent exists (PostUpdateMigrator.ts:1216-1241 conformance auto-invoke; :1395-1420 bundled-copy + fingerprint). → extend :1395 bundled-copy migration table for both §3.6 edits; list in deliverables.
INT1-4 MAT [§3.2/3.3] Standard-B declaration incomplete: needs per-watcher structured block — max-attempts, max-wall-clock, backoff, dedupe-key, breaker, units max-notification-latency, audit-location, remediation-actions, severity class — for reconciler breaker item, intended-owner-ambiguity item, error-arm fail-open item; silence ceiling has no value/units.
INT1-5 min [posture hold row] physical-credential-locality contestable (bot credential IS secret-synced; local thing is the live long-poll delivery attempt); silent on crash-mid-hold loss (queue dark → in-memory hold dies) contradicting silent-loss-refusal-conservation. → re-tag/defend + state loss bound honestly.
INT1-6 min [§3.1/§4] config knobs: shape ({enabled,dryRun}?), read semantics, third flag's config path, DEV_GATED_FEATURES rows (precedent ConfigDefaults.ts:1360-1367). → name all three paths + shape + read-time + rows.
INT1-7 min [§3.5] graded-review job maturation path undeclared (gate flagged) → one sentence: dev-agent soak → fleet criteria, or explicit operator-enabled exception.
INT1-8 min [§3.2/3.5] no observability surface: reconciler status route (GET /pool/duplicate-reconciler precedent) + provenance route name + dashboard placement beside duplicateTopics badge.
INT1-9 min [§3.5/posture] backup posture undeclared for provenance files + breaker-counter store (precedent: "aggregates backup-excluded"). → one line each.
INT1-10 min [§3.2 step 2] pin repairs do NOT go through SessionOwnershipRegistry CAS verbs — pin store uses U4.1 HLC fold + quarantine path (different conflict semantics). → name actual pin-store write path + quarantine interaction.

#### lessons-aware (internal, round 1) — VERDICT: FINDINGS (6 MATERIAL, 6 minor)
LA1-1 MAT [§3.2.6/ReapGuard] Layer-D subsumption FALSE for the Mini-side shield: F8 carve-out lifts ONLY the lease gate; closeout terminate re-checks every other KEEP-guard incl. open-commitment fresh (SessionManager.ts:1284-1294) → converged record still vetoes 5× → breaker → manual heal = the exact 2026-07-10 outcome. Mirror-gates lesson (PR #1125), P14. → survivor floor moves/absorbs topic-scoped commitment to surviving copy before closeout, OR narrow duplicate-reconciled single-reason bypass for open-commitment (mirror bypassRecentUserMessageForConfirmedMove, liveness-confirmed owner copy required); correct §3.2.6 either way.
LA1-2 MAT [§3.2/posture/foundation] "EXISTING CAS + replication" asserted as given but fleet-default substrate is InMemorySessionOwnershipStore (wiped on restart — regenerates the no-record state); durable Local store only under replication-on/dev-gate (server.ts:18966-18979); known false-positive history (cross-machine transfer not wired, 2026-06-15); §5 stubs the peer relay = the replication hop is what's stubbed. → affirmative two-node evidence that a lease-holder CAS write lands in the PEER's registry (closeout reads peer's own ownerOf); gate Layer-B enforcement flip on durable+replicated substrate live.
LA1-3 MAT [§5] wiring-integrity tests missing for new DI components while the incident's root cause IS a no-op injected dep (handleLocally, server.ts:21175). → wiring tier: assert SpawnAdmission consulted at each callsite with non-null non-no-op deps delegating to real registry/ladder; provenance writer actually wired.
LA1-4 MAT [§3.6] Migration Parity confirmed (same as INT1-3): PostUpdateMigrator.ts:1216-1241 + :1395-1410 precedents; add both idempotent migrations to deliverables, same PR.
LA1-5 MAT [§3.3 rung 2/foundation] claim rung depends on StaleOwnerReleaseEngine.actForceClaim which ships dryRun on fleet (U4.2) — with it dark a provably-dead owner is never claimed, rung 3 loops notices forever; G3 dark-but-load-bearing. → declare dependency posture (loadBearing+criticalPath on /guards, or rollout coupling: Layer-A enforcement no earlier than stale-owner-release leaving dryRun on that pool); state honest rung-3 behavior when claim rung inert.
LA1-6 MAT [§7 JWF] article inverts "The Body and the Mind" (mind holds final authority; structure is signal) + overlaps Signal-vs-Authority & Tiered Development (audited below-floor); §0 gap claim overstated. → Derives-from line reconciling: floors are the S-v-A exemption class (enumerable-domain invariants + safety guards on irreversible actions); state coexistence with Tiered Development's audited below-floor authority; soften §0 claim.
LA1-7 min [§3.5/FD7] Maturation Path: declare DARK_GATE_EXCLUSIONS-style category+reason (cost-bearing: budget-capped LLM replay) + registered exclusion entry.
LA1-8 min [whole-spec] Agent Awareness: auto-heal reverses documented "flag not heal" behavior; new user-visible surfaces → generateClaudeMd + migrateClaudeMd updates in deliverables.
LA1-9 min [frontmatter] missing parent-principle (precommit gate requires resolvable registry article) + lessons-engaged block. → add parent-principle: Signal vs. Authority / The Body and the Mind; lessons-engaged list.
LA1-10 min [§7 DPOR] overlaps Observable Intelligence → Derives-from line justifying separate article (content vs metadata) or propose as ratified sharpening.
LA1-11 min [§3.2] P7: reconciler has no supervision tier declaration → declare tier 0 with justification (policy decisions route to J2/attention) or tier 1.
LA1-12 min [§3.3 rung 3] P17: notice cardinality unpinned under burst → per-topic/per-episode dedup + burst E2E asserts notice count bound.

#### adversarial (internal, round 1) — VERDICT: FINDINGS (9 MATERIAL, 8 minor)
ADV1-1 MAT [§3.2.6] = LA1-1: F8 lifts ONLY lease gate (SessionManager.ts:1344-1360); open-commitment KEEP topic-scoped, re-armed by owner-side traffic (ReapGuard.ts:66-79,183-193) → active-conversation duplicate vetoed 5×→breaker→attention, not auto-heal. → duplicate-reconciled commitment carve-out conditioned on liveness-confirmed owner copy, or state honestly work-guarded dups end at breaker.
ADV1-2 MAT [§3.2] healer depends on the channel whose failure caused the incident: convergence lands in lease holder's LOCAL CAS, reaches peer only via journal replication (server.ts:18940-18990, OwnershipApplier reads REPLICATED journal; incident trigger WAS a bloated journal). → direct authenticated mesh verb (remote-close precedent) or peer-registry echo check: confirm peer OBSERVED converged record within bound, else escalate.
ADV1-3 MAT [§3.1/3.2.1] symmetric divergence (both say self) unaddressed: concurrent unowned→placeAndClaim on two machines both succeed (per-machine CAS, async replication); rule 2 "most recently recorded" = wall clock across skewed machines (pin-quarantine exists because skew corrupted pins once). → both-self as first-class reconciler input; resolve on ownershipEpoch (monotonic, CAS-native), never recency timestamps.
ADV1-4 MAT [§3.1] error-arm unbounded + shares fault domain with healer: broken registry → EVERY inbound EVERY machine spawns locally (mass dups) while reconciler CAS + closeout gate read the same broken store — gate and healer fail together. → per-topic-once + error-arm breaker (K consecutive errors → degrade error to rung-3 queue+notice floor); specify reconciler/closeout behavior under registry error.
ADV1-5 MAT [§3.4 J2] self-authored tail poisoning: runaway session prints fabricated "live run/critical work" claims; untrusted-envelope prevents instruction-following, NOT evidence-weighting; move-ownership is floor-legal. → mechanical corroboration (registered run state, CommitmentTracker, server-side timestamps) required before move; tails advisory only, never sufficient evidence. (= SEC1-2 strengthened)
ADV1-6 MAT [§3.2.1] input freshness unpinned: WS4.4(f) pool-cache load-shed serves stale:true rows; converging on stale row (session exited) rewrites ownership against reality; closeout liveness gate confirms machine reachability, not live session. → fresh direct probe (never cache/stale) + target-has-live-copy precondition before any record write.
ADV1-7 MAT [§3.2.5/3.3] breaker asymmetry = denial lever: breaker freezes healer but rung-2 claim-then-spawn generator keeps firing on flapping owner → fresh unhealed dups while attention deduped. → breaker-open clamps the ladder for that topic (skip rung 2, route rung-3) until closed or operator acts.
ADV1-8 MAT [§4] per-machine flag skew undeclared with documented prior incident (pool-consistent-activation fix, server.ts:18952-18963, seat-died-on-arrival 2026-06-16); lease on dryRun machine → no reconciliation at all; fleet defaults (InMemory, no replication) make other-alive/dark unresolvable. → declare both flags pool-consistent (same activation-signal pattern); precondition Layer B on durable store + replication; add flags to machine-coherence guard's coherence-critical set.
ADV1-9 MAT [§3.2.4/3.2.1] both-copies-carry-live-runs contradicts floor ("owner survives" vs "run-carrier never closed, ownership MOVES"); reconciler-driven transfer gets 409 needsConfirmation (consent gate an autonomous reconciler can't answer). → static rule: both-run → escalate-to-attention (no auto-converge); reconciler 409 → escalate, never retry.
ADV1-10 min [§3.2.3] duplicate-reconciled close of work-carrying copy mints mid-work resume-queue entry (garbage for another system to collect; J2 move can flip drain-time check). → such closes never resume-queue-eligible at enqueue.
ADV1-11 min [§3.3 rung 3] flapping owner → notice per wobble; cap notices per-topic cooldown (reap-notice precedent). (= SC1-7)
ADV1-12 min [§3.4 J2] no cap on consecutive defer-one-tick → immortal duplicate via floor-legal choices. → force deterministic default after N defers.
ADV1-13 min [§3.4 J1] "message urgency" content-derived → envelope as untrusted; provenance records it as such. (= SEC1-6)
ADV1-14 min [§3.4/3.5] J2 cross-machine tail fetch = new egress; provenance stores ANOTHER machine's tail (PII beyond credentials) → redact on SERVING machine before wire; pin tail-slice bound; resolve PII OQ before Increment 3.
ADV1-15 min [§3.2 step 2] registry-CAS + pin-repair ordering/atomicity unspecified; crash between writes recreates §2.3 two-source disagreement. → write order + idempotent re-repair next tick.
ADV1-16 min [§3.1] TOCTOU router route() at :2484 vs seam resolve at spawn callsite — two resolutions can disagree mid-dispatch. → seam consumes router's outcome for same message when present, or re-CAS idempotently under same nonce.
ADV1-17 min [§3.2.5] breaker counters reset on lease move → chronic re-dup topic evades breaker under lease flap. → persist per-topic episode counts in replicated store or bound resets/24h.

## Round 1 → v2 resolution summary
All 30 material findings folded in ONE coherent rewrite (commit 08c2c7ec4): §3.2.4a commitment custody re-bind + narrow two-guard carve-out; §3.2.0 substrate preconditions + pool-consistent activation + machine-coherence set; §3.2.3 peer-echo confirmation + pin-store real write path + write ordering; §3.2.2 evidence-ordered intended-owner (pin → ownershipEpoch → server-registered run → J2/escalate; recency deleted; both-self first-class; both-run escalate; 409 escalate); §3.2.1 own tick + fresh-probe + per-tick caps + partition honesty; §3.2.5 replicated breaker counters + flip counting + ladder clamp; J2 mechanical-corroboration floor + defer cap; J1 episode caching + 5s timeout + envelopes both arbiters; §3.5 provenance denylist/0600/backup-excluded + redaction invariant + async writes + clamps + named route; §3.3 stage matrix + honest per-state wording + G1 path with declared election exception + notice dedup + silence ceiling 10min + hold caps; §3.1 error-arm bounds/breaker + TOCTOU single-resolution + cached-view pin; §3.6 migrations + agent-awareness; §3.7 alternatives-considered (both externals); §3.8 Standard-B blocks; §7 Derives-from reconciliations; FD 1-13 frontloaded; §8 deliverables.

## Round 2 — 2026-07-10 ~20:25 PDT
- Body hash at external pass: 1d9f474631e77d6945fe31179bcf0bd46ce834ff5d18b6479433947ee856852a (changed → externals mandatory, running)
- Standards-Conformance Gate: ran (1 flag) — round-1's 3 flags CLEARED; new: Cross-Machine Coherence vs error-arm fail-open (deliberate defended trade — handed to reviewers)
- Internal reviewers (6): launched, session model claude-fable-5
- External: codex-cli + gemini-cli launched on v2
- External r2 results: codex-cli:gpt-5.5 ok MINOR ISSUES (5); gemini-cli DEGRADED (timeout) — per-round partial, spec-level flag remains clean via codex + r1 gemini success
codex-r2-1 [§3.3/§4/FD5] rollout contradiction: fleet matrix row vs §4 "enforcement requires queue+release live" — may Layer A enforce fleet-wide notice-only, or blocked? → state explicitly.
codex-r2-2 [§3.5] full-context provenance = high privacy surface; consider opt-in/hashes+excerpts → defend: the logged context IS the bounded chokepoint-redacted form the model saw (never more), + sampling + retention; make explicit.
codex-r2-3 [§3.1-3.7] complexity vs core bug: add minimal-viable-fix comparison → fold as §3.7 alternative: increments 1-2 ARE the minimal deterministic fix; arbiters removable per graded review; standards ride per operator directive.
codex-r2-4 [§2.6/3.2.0/3.2.3] substrate-readiness flapping: define degraded-mode behavior (gate fail toward what? reconciler freeze?) → add explicit degraded-mode paragraph.
codex-r2-5 [glossary] terminology load → add short glossary.

#### scalability r2 — VERDICT: FINDINGS (3 MATERIAL, 5 minor)
SC2-1 MAT [§3.2.4a] commitment re-bind = cross-machine mutation with no posture/failure semantics: CommitmentTracker per-machine (WS2 excludes commitments), open commitment on NON-owner, surviving session on owner — local re-bind beacons a nonexistent session OR unnamed transport. → posture-table row; executor (closing machine, pre-close hook); transport (authenticated mesh write to owner's tracker vs local re-bind + owner-side adoption); 5s timeout; fail → defer close one tick, counts toward 3-attempt cap.
SC2-2 MAT [§3.2.4a/3.2.3] carve-out live-owner probe TOCTOU-stale at terminate: probe is reconciler pre-write; closeout fires ≥2 ticks later on non-owner; owner crashes in window → zero live copies + commitment bound to dead session. → CLOSING machine re-runs live-owner probe at terminate time; fail/timeout → veto carve-out (KEEP), defer.
SC2-3 MAT [§3.2.3.2] echo bound 2 ticks=120s uncalibrated vs journal-hop latency (applier materializes 15s but hop latency unverified); mass-heal → per-topic escalation flood. → echoConfirmTicks config sized to measured hop+applier; aggregate per-tick convergence-not-observed into ONE item enumerating topics (burst test asserts count).
SC2-4 min [§3.2.1] ws44PoolCache is dark-gated — fleet flip would ride a dark cache → declare cache-dark fallback (lease-holder-only direct fan-out N-1/60s) or add flag to arming preconditions.
SC2-5 min [§3.2.1] fresh probes: 5s budget; fail → defer topic, counts toward 3 attempts.
SC2-6 min [§3.2.3.2] echo per EVERY non-owner copy (3+ machines); escalation names unconfirmed machines.
SC2-7 min [§3.5/§4] sampling 1.0 soak-exit trigger: Increment-2 entry drops to 0.1, or per-day byte cap.
SC2-8 min [§3.2.5] breaker records ride the journal whose bloat caused the ancestor crash → one sentence: journal post-fix size bounding covers the new volume.

#### adversarial r2 — VERDICT: FINDINGS (5 MATERIAL, 4 minor)
ADV2-1 MAT [§3.4] J2 corroboration floor SELF-MINTABLE: the incident's duplicate CARRIED an open commitment; any dup that opens a commitment (routine per CLAUDE.md mandate) + poisoned tail satisfies the floor. → corroborators must PREDATE episode-open (createdAt/registration vs episode-open); post-episode mints never corroborate; valid classes = pre-episode server-registered runs + live work-gate probe.
ADV2-2 MAT [§3.2.4a] re-bind doesn't move custody in the incident geometry: CommitmentTracker per-machine, beacon machine-local, re-bound record stays on CLOSING machine whose sends the election silences (owner-other) → promise rots. Same gap for resolveUnansweredInbound re-injection (local machinery, remote survivor). → custody transport mechanically: mint on owner machine via authenticated API + idempotent externalKey; local record superseded (beacon-suppressed, audited) only on ack, escalate on no-ack; defined cross-machine inject path (or durable-queue custody) for unanswered inbound.
ADV2-3 MAT [§3.2.4a/3.2.3] carve-out caller-forgeable + probe timed wrong: F1/F8 precedents are plain booleans on terminateSession; "single-disposition" is only a reason string. → (a) sweeper honors carve-out only when reconciler-minted lease+epoch-fenced convergence record for this topic/episode present in registry at terminate; (b) live-owner probe re-executes at terminate time, veto on failure. (= SC2-2 + provenance verification)
ADV2-4 MAT [§3.2.2] epoch comparison breaks under backup restore: restored machine's genuine self-claims land below a peer's stale record; rule 2 resolves before rule 3 → actively-serving copy closeable. → boot-time epoch high-water re-seed from REPLICATED journal before participating in placement; rule-2/rule-3 contradiction (epoch winner ≠ live-run holder) escalates, never converges.
ADV2-5 MAT [§3.1.3e/3.8] consecutive-K breaker defeated by flapping registry (interleaved successes): each success closes episode, each error re-opens → unbounded-over-time fail-open spawns + item churn. → windowed rate trip (≥N in M min regardless of interleaving); episode-close hysteresis (J consecutive clean); ≥3 episodes/24h/machine → HIGH + clamp to notice floor.
ADV2-6 min [§3.4] defer-cap terminal ambiguous → pin to escalate-to-attention (3 defers = evidence of ambiguity, not license to close).
ADV2-7 min [§3.3.3] final owner-liveness re-check immediately before notice send (shrink contradictory-voices window to probe RTT).
ADV2-8 min [§3.3/3.7] state the honest user-visible trade: wrong-machine instant answer splits the conversation + mints immortal duplicate — worse than one resend; add to §3.7/FD for the fleet flip.
ADV2-9 min [§3.2.3] echoed-but-unhealed (lying echo) → name next-tick persistent-duplicate re-detection as backstop; count toward §3.2.5 breaker.

#### decision-completeness r2 — VERDICT: FINDINGS (1 MATERIAL, 6 minor); COUNTS: frontloaded=13 cheap-tags=1 contested-cleared=1 contested-rejected=0
DC2-1 MAT [§3.2.4a] re-bind unbuildable as stated: Commitment record has topicId only, NO session/machine field (CommitmentTracker.ts:35-59, no rebind/adopt path); stores machine-local, not replicated; posture table omits commitment-custody row. → frontload mechanics: owner-side ADOPT (reconciler reads closing copy's open commitments over authenticated mesh read; owner tracker mints deduped equivalents via externalKey; source records mutate() to terminal 'rebound' state, audited); posture row; executor machine; owner-unreachable-at-close → defer close. (= SC2-1, ADV2-2 convergent)
DC2-2 min [§3.2.3.2] "2 ticks" unqualified; peerEchoConfirmTicks + reconciler tick interval not in §4 keys → declare both.
DC2-3 min [§0/FD1] partial-ratification mapping: declined checkbox → dropped deliverable set, one sentence per standard.
DC2-4 min [§3.4/FD10] "FD5b" collides with spec's own FD numbering → cite natureRoutingMap.ts injection-exposure registry instead.
DC2-5 min [§3.2.2 rule 3] name the read channel keeping rule 3 distinct from self-report (journal-replicated registration snapshot vs authenticated live probe).
DC2-6 min [FD11/§3.8] "§3.1.3e" anchors a lettered sub-item that isn't lettered (×2) → letter the bullets or fix anchor; name concrete log file for <journal ref>.
DC2-7 min [§5/§4] two-node harness execution surface: declare CI E2E vs manual gate owner.

#### security r2 — VERDICT: FINDINGS (3 MATERIAL, 5 minor) + error-arm fail-open judged SUFFICIENT (bounded, loud, breaker to notice floor, reconciler freezes same fault domain)
SEC2-1 MAT [§3.5/posture] "never-served denylist" cited precedent DOESN'T EXIST: .claude/hooks/ is NEVER_EDITABLE_PREFIXES (fileRoutes.ts:188) — blocks EDITS only; reads/download still serve; allowedPaths ['./'] serves state/. → respecify as NEW read-side NEVER_SERVED_PREFIXES enforced inside validatePath (list/read/download); name as net-new mechanism; wiring test: GET state/judgment-provenance/* → 403.
SEC2-2 MAT [§3.2.4a] re-bind collides with commitments replication security model: single-writer per origin machine, read-only replication, forged-origin-row rejection (CommitmentsSync "first-hop with teeth") → cross-machine re-bind = undeclared shield-stripping write primitive; no compensation if close vetoed (shield stripped, commitments moved, duplicate still running). → re-bind executes locally on closing machine's tracker; cross-machine custody = close-and-remint on survivor via authenticated mesh verb preserving origin-stamp invariant; re-bind ROLLBACK on vetoed close. (= SC2-1/ADV2-2/DC2-1 convergent cluster)
SEC2-3 MAT [§3.2.4a] carve-out TOCTOU (= SC2-2/ADV2-3): probe at record-write time, close ≥2 ticks later on peer; contradicts spec's own §7 "prove at fire time" + the mirrored precedent is same-tick (SessionManager.ts:1270-1279). → closing machine re-probes at terminate inside carve-out evaluation; fail → veto.
SEC2-4 min [§3.3.3/posture] election-bypass dedupe is machine-scoped (owner-dark:<machine>:<episode>); split-brain two front doors → two notices = competing voices. → topic-scoped suppression check before send, or weaken claim.
SEC2-5 min [§3.2.2 rule 2] epochs are peer-attested; inflated/third-machine-attributed rows → intended owner → carve-out kill. → state trust assumption; first-hop origin-stamp validation on ownership-journal rows before epoch admissible.
SEC2-6 min [§3.2.5] replicated counter rows need receive-side type-clamping (NaN/2^53/absurd timestamps → permanent rung-3 clamp = per-conversation DoS). → standard clamp contract.
SEC2-7 min [§3.2.2/3.2.3] J2/convergence-not-observed/both-run escalation bodies unpinned → fixed templates + metadata (reason enum), never free text (injection relay into operator context).
SEC2-8 min [§3.5] dir mode 0700 + gitignored declared (agent home is a git repo — a committed row outlives retention).

#### integration r2 — VERDICT: FINDINGS (4 MATERIAL, 3 minor) + round-1 folds all verified grounded
INT2-1 MAT [§3.2.4a] Commitment has NO session binding field (topicId/boundTuple/boundBy only, CommitmentTracker.ts:35-75); ReapGuard veto is activeCommitmentForTopic(topicId) — TOPIC-scoped; nothing session-shaped to CAS; per-machine store, in-process single-writer queues; survivor's beacon can't drive a record it doesn't hold; maxActiveBeacons boot-cap on transfers unaddressed. → REDESIGN: the two-guard carve-out is the operative piece (topic-scoped guard means record stays valid for topic); replace phantom session re-bind with explicit disposition of the residual open record on the closing machine (owner-gated suppression at PromiseBeacon.emitUserSend chokepoint + audited ownership annotation), OR specify real cross-machine custody incl. beacon-cap. (final shape of the SC2-1/ADV2-2/SEC2-2/DC2-1 cluster)
INT2-2 MAT [§3.8] recoverableLatencyCeiling proposed at 300s (three-standards-enforcement.md:317-330), key not yet landed → fails CLOSED (watcher PR blocked until it lands); reconciler 30min = 6× over, ladder 10min = 2× over; ladder row conflates user-facing rung-3 notice with operator-facing Standard-B latency (different audiences). → add ≤300s low-noise "self-heal in progress" operator observability line per Backstop 1 to both watchers (30min/10min stay as escalation-proper); registry-key landing = hard sequencing dependency of Increment 1.
INT2-3 MAT [§3.5] (= SEC2-1) never-served denylist doesn't exist; blockedFilenames is config-MUTABLE via PATCH /api/files/config; NEVER_EDITABLE blocks edit only. → new hardcoded NEVER_SERVED_PREFIXES in canonicalizePath/read routes (config-immune), §8 deliverable + wiring test; correct the precedent claim.
INT2-4 MAT [§3.3] stage matrix rows 2-3 overstate rung 1: holdForStability trails ONE STAGE behind the queue (durable-inbound-message-queue.md:7) — at "dev pool queue live" hold is still dry → 90s hold doesn't exist; ceiling calibration + Increment-2 shape inherit the error. → hold-policy liveness as its own matrix dimension/precondition; queue-live rows are rung 3 with durable custody until holdForStability itself live.
INT2-5 min [§3.2.0] machine-coherence guard itself dev-gated dark + dryRun-first (same G3 class) — declare per-stage posture honestly; pool-consistent activation is the PRIMARY defense (manifest COHERENCE_CRITICAL_FLAGS extensible, lint requires DEV_GATED_FEATURES listing — mechanically fits).
INT2-6 min [§3.8] error-arm class justification should argue the shared-fault-domain honestly (duplicates formed mid-episode unhealable until recovery; external side effects irreversible) — class defensible via K=5 bound + post-recovery convergence.
INT2-7 min [§4] Increment 2 INHERITS inboundQueue's own ≥7d/counter gates + U4.2's own soak call — state it stalls honestly behind them (current wording reads as if this spec flips them).

#### lessons-aware r2 — VERDICT: FINDINGS (4 MATERIAL, 4 minor); JWF Derives-from verified GENUINE (cite the S-v-A doc section)
LA2-1 MAT [§3.2.4a/foundation] CommitmentsSync is READ-replication only (origin-keyed, replicas not driveable); mutate() is origin single-writer; PromiseBeacon drives origin-local store, self-suppresses on non-owned topics (PromiseBeacon.ts:151,698-703); sessionEpoch mismatch → false terminal 'session-lost' violation risk. → survivor-side successor commitment + origin close w/ cross-ref (or custody-transfer verb extending COMMITMENTS-COHERENCE-SPEC); define sessionEpoch handling; two-node integration test explicit.
LA2-2 MAT [§3.5/FD6/foundation] = SEC2-1/INT2-3 (phantom denylist): /api/files/read (:381) + download (:697) check only validatePath+blockedFilenames; NEVER_EDITABLE enforced only at the two EDIT paths (:502,:671). → BUILD invariant read-path deny covering read/download/link + WS4.4 peer-proxy; state net-new.
LA2-3 MAT [§3.2.3/3.2.4a] mirror-gates engaged against the WRONG mirror: sibling topic-moved disposition through the SAME sweeper has a DIFFERENT gate set (only bypassRecentUserMessage; no commitment carve-out/custody) — deliberately-moved topic w/ open commitment leaves immortal old copy healed later as reconciler episode, counting toward P19 breaker → operator move patterns can clamp the ladder. → extend custody+carve-out to topic-moved, or justify asymmetry AND exclude transfer-traceable duplicates (reap-log reason) from breaker count.
LA2-4 MAT [§0/§3.1] Cross-Machine Coherence flag unengaged in the spec text (design sufficient, never argued). → §0 row + §3.1 argued-exemption clause: bounded, loud, named converge-back path (Layer-B on registry recovery).
LA2-5 min [§7 JWF] add explicit cite: docs/signal-vs-authority.md §"When this principle does NOT apply".
LA2-6 min [§3.8/FD11] "§3.1.3e" anchor doesn't exist → fix refs.
LA2-7 min [§3.3/3.4 J1] urgency inert after episode's first message under caching → add urgency-class change to re-invocation triggers (or drop urgency).
LA2-8 min [§2.6] SpeakerElection owner-liveness-blindness beyond the rung-3 notice: every other suppressed send defers to a dark owner indefinitely — declare disposition (tracked follow-up) per Close the Loop.

## Round 2 material clusters (deduped) → v3 resolutions
A. Commitment custody (SC2-1+ADV2-2+SEC2-2+DC2-1+INT2-1+LA2-1): kill phantom session re-bind → post-close custody transfer: reconciler reads closing copy's open topic commitments (authed mesh read); OWNER machine mints successor commitments (own API, idempotent externalKey, origin-stamp invariant preserved); origin records → terminal 'superseded-by-ownership-move' (own CAS, audited) ONLY on successor ack; no-ack → escalate; vetoed close → nothing transferred (transfer is post-close); PromiseBeacon owner-other self-suppression covers the gap; sessionEpoch: supersede disarms session-lost; beacon boot-cap: successor respects existing activation cap; posture row; two-node test.
B. Carve-out forgery+TOCTOU (ADV2-3+SEC2-3+SC2-2): sweeper honors carve-out only w/ reconciler-minted lease+epoch-fenced convergence record present at terminate + terminate-time live-owner re-probe (veto on fail/timeout).
C. Phantom denylist (SEC2-1+INT2-3+LA2-2): net-new hardcoded NEVER_SERVED_PREFIXES in validatePath covering list/read/download/link + WS4.4 proxy; config-immune; wiring test GET→403; correct claim.
D. J2 corroboration self-mintable (ADV2-1): corroborators must PREDATE episode-open; valid classes = pre-episode server-registered runs + live work-gate probe.
E. Epoch regression (ADV2-4): boot-time high-water re-seed from replicated journal before placement; rule2≠rule3 contradiction → escalate. + SEC2-5 origin-stamp validation for admissible epochs.
F. Error breaker flapping (ADV2-5): windowed rate trip ≥N/M-min + close hysteresis J-clean + ≥3 episodes/24h → HIGH+clamp.
G. Standard-B ceiling (INT2-2): ≤300s low-noise operator line both watchers; 30min/10min = escalation-proper; registry-key landing = Increment-1 dependency; audiences separated.
H. Stage matrix hold dimension (INT2-4): holdForStability trails one stage — own dimension; queue-live rows = rung-3-with-custody until hold live.
I. Wrong mirror (LA2-3): extend custody+carve-out to topic-moved disposition (same sweeper machinery); transfer-traceable episodes excluded from breaker.
J. Cross-Machine Coherence engagement (LA2-4): §0 row + argued exemption (security r2 already judged sufficient).
K. codex-r2: enforcement-requires-queue-live pinned (fleet row = dryRun-descriptive); provenance defense (log = exactly the bounded redacted form the model saw); minimal-viable-fix alternative in §3.7; substrate-flap degraded-mode; glossary.

## Round 3 — 2026-07-10 ~20:45 PDT
- Body hash: 5f9a6fae365674b16f8cf349bf1103534b31a4ef5238f7dba9d275f07d060bdd
- Standards-Conformance Gate: ran (0 flags) — ALL cleared incl. Cross-Machine Coherence (argued exemption accepted by the code-backed gate)
- External r3: codex ok verdict=SERIOUS ISSUES (5); gemini ok verdict=SERIOUS ISSUES (3). Both RAN (real passes).
codex-r3-1 + gemini-r3-1 [§3/§3.2/§3.7] "homegrown consensus" — ACTIONABLE CORE: add formal consistency contract (safety/liveness properties, NON-goals: no linearizable ownership, no guaranteed single-owner under partition, fail-open can duplicate side effects; partition/delayed-replay test matrix). NOTE: design uses EXISTING lease/CAS/journal (production substrate), not a new system — contract makes the boundary explicit instead of implied.
codex-r3-2 [§3.1 row e] fail-open vs invariant — re-statement of the argued exemption (security-r2 judged sufficient; gate cleared it). Fold: precision wording — one-owner is enforced in the resolvable path and CONVERGING under faults; stated in the consistency contract. Documented dissent on the stateless-intake alternative (trades reachability).
codex-r3-3 [§3.2.4a] custody transfer = second feature → own design boundary/rollout gate. FOLD: sub-flag commitmentCustodyTransfer (independently reversible), Increment 2b with own two-node E2E + rollback; disabled ⇒ open-commitment duplicates ESCALATE instead of auto-close (safe degradation).
codex-r3-4 [§3.4/§7] don't ratify JWF until shadow data — CONTRADICTS operator directive (19:40 go: standards ratify in this cycle; FD1). Documented dissent in report; arbiters already removable on graded evidence.
codex-r3-5 [§3.5] retention 30d aggressive; encrypt-at-rest; opt-in full capture. PARTIAL FOLD: retention default 30→14d; encrypt-at-rest = considered-not-adopted note (posture parity with all machine-local logs; vault encryption not streamable); opt-in full capture REJECTED (guts DPOR always-on provenance; bounded-form honesty + short retention is the middle) — documented.
gemini-r3-2 [J1 unnecessary] re-litigation of §3.7 alt-3 (J1 = optimization inside deterministic default, shadow-first, removable on evidence) — resolved stands; documented.
gemini-r3-3 [opacity] FOLD: expand glossary (F8, WS4.4(f), U4.1 HLC fold, fenced lease, journal replication).
- scalability r3: CONVERGED-CLEAN. All r2 folds verified genuine at load-bearing sections. New-cost audit: all v3 additions bounded by existing caps. 2 minors: SC3-1 terminate re-probe inherits 5s budget + must not block sweeper; SC3-2 rotation/size bound declared for logs/duplicate-reconciler.jsonl + logs/owner-dark-ladder.jsonl.
- security r3: FINDINGS = 2 minors only, zero material; all r2 folds verified genuine (grep-confirmed premises). SEC3-1 min: NEVER_SERVED_PREFIXES must evaluate against fully-resolved realpath (symlink evasion) + symlink case in wiring test. SEC3-2 min: custody successor-mint API type/length-clamps + envelopes peer-derived free text (userRequest/agentResponse) like every replicated receive path.
- decision-completeness r3: FINDINGS = 4 minors, ZERO material; COUNTS frontloaded=15 cheap=4 contested-cleared=4 rejected=0; open questions genuinely empty; all r2 folds builder-complete. DC3-1 min: pin J2 corroboration boundary = CONTESTED COPY's spawn timestamp (episode-open at first-detection stays for breaker/dedupe). DC3-2 min: pin custody verb API shape (reuse POST /commitments + externalKey) + which origin fields the successor carries. DC3-3 min: ≤300s observability line = fixed-template metadata row in audit-location + status surface, never a push (quiet-by-default). DC3-4 min: error-arm breaker bounds declared code constants (safety-bound precedent), not config keys.
- adversarial r3: FINDINGS = 2 MATERIAL + 3 minors (both material are REAL holes in v3's own additions):
ADV3-1 MAT [§3.2.4a] mirror-parity leg unsatisfiable: topic-moved is transfer-written via normal placement CAS — no reconciler-minted fenced record exists, so carve-out condition (i) can never pass on that leg (moved+open-commitment stays immortal) or implementers loosen the fence (reopens forgery). → transfer-executor-minted, equivalently-fenced move-disposition record (same provenance shape/episode scoping) as the ONLY accepted fence on that leg + forgery-rejection test extended.
ADV3-2 MAT [§3.4] work-gate-probe corroboration arm self-generates post-episode by construction (spawn IS episode-open; answering the misrouted message passes the probe); episode-open never operationally timestamped. → episode-open = contested copy's spawn-registration timestamp (backdated at detection); work-gate probe corroborates ONLY defer-one-tick/close-veto, never move. (matches DC3-1)
ADV3-3 min: served-deny must imply edit-deny (one hardcoded list, both chokepoints, edit-path wiring test) — provenance poisoning feeds graded review + batteries otherwise.
ADV3-4 min: terminal-supersede fenced to verified successor (externalKey + owner machineId + mesh-read existence check before CAS) — any Bearer caller could otherwise silently drop a live promise.
ADV3-5 min: ≥N transfer-traceable episodes/topic/24h → ONE observability item (never ladder clamp) — flapping mover otherwise unbraked by this spec.
ADV3-6 min: J1 re-invocations capped ≤3 per (owner-machine, episode) — urgency-class oscillation is content-drivable spend.
- integration r3: FINDINGS = 1 MATERIAL + 2 minors; all r2 folds verified genuine (grounding spot-checked); CI two-node harness judged FEASIBLE (replication hop = localhost-mesh HTTP; tmux may be shimmed).
INT3-1 MAT [§8/§3.6] gitignore entry has NO migration-parity path: fleet agent homes are git repos, state/ is untracked-but-NOT-ignored (verified via git check-ignore); broad git add commits provenance rows cross-machine, defeating machine-local contract. → GITIGNORE_ENTRIES/ensureGitignore (fresh) + idempotent PostUpdateMigrator gitignore patch (existing; .worktrees/ precedent PostUpdateMigrator.ts:2532-2549); list beside the other migrations.
INT3-2 min [§3.2.4a/§8] name the origin-side supersede verb in §8 + evidence gate (successor-ACK ref + fenced convergence record; preserves origin single-writer CAS; supersededBy precedent CommitmentTracker.ts:121).
INT3-3 min [§3.8/§4/§8] ceiling-key authority: Increment 1 lands the key only at three-standards-enforcement's converged value; if unconverged at build time, proceed under fail-closed interim, landing rides that spec's cycle (drop "hard").
- lessons-aware r3: 1 MATERIAL (LA3-1 = ADV3-1 topic-moved fence — fix: the transfer-minted CAS+epoch-fenced ownership record in the local registry view satisfies condition (i) for that disposition, never a reap-reason string) + 3 minors: LA3-2 lessons-engaged add P20-P23; LA3-3 capacity-safety ratchet (selfActionRegistry registration + convergence test) in §5; LA3-4 origin-side supersede verb in §8 (= INT3-2).

## Round 3 → v4 fold plan (5 deduped material + 17 minors)
M1 (ADV3-1+LA3-1): topic-moved fence = transfer-minted CAS+epoch-fenced ownership record; forgery test covers the leg.
M2 (ADV3-2+DC3-1): episode-open = contested copy's spawn-registration ts (backdated at detection); work-gate probe corroborates ONLY defer/close-veto, never move.
M3 (INT3-1): gitignore via ensureGitignore + PostUpdateMigrator patch (.worktrees/ precedent).
M4 (codex/gemini-r3-1): §3.0 consistency contract — safety/liveness + explicit NON-goals (no linearizable ownership; no guaranteed single-owner under partition/error; fail-open may duplicate side effects transiently; invariant = enforced in resolvable path, converging under faults) + partition/delayed-replay row in §5.
M5 (codex-r3-3): custody transfer = own sub-flag commitmentCustodyTransfer + Increment 2b + safe degradation (disabled ⇒ open-commitment dups escalate, never auto-close).
Minors: SC3-1/2, SEC3-1/2, DC3-2/3/4, ADV3-3/4/5/6, INT3-2/3, LA3-2/3/4, codex-r3-5 (retention 14d + at-rest note), gemini-r3-3 (glossary). Dissents documented (report): codex-r3-2 (stateless-intake alternative), codex-r3-4 (defer ratification — operator directive governs), gemini-r3-2 (J1 existence), codex-r3-5 (opt-in full capture rejected).

## Round 4 — 2026-07-10 ~20:50 PDT
- Body hash: 0b96fe91f1b839fc5fdf7fba8ff51e08271b1ff21ae2d04dece3c073b43af877
- Gate: ran (1 flag) — Cross-Machine Coherence RE-triggered by §3.0's own honest non-goals language (disclosure paradox; §0 argued exemption stands; handed to integration r4 for final ruling)
- External r4: codex ok MINOR ISSUES — "No serious blocker found." 5 editorial asks: (1) §8 core-fix vs ecosystem labeling; (2) §3.7 topic-owned-commitments alternative entry; (3) §4 Increment-4 entry = shadow metrics must BEAT static policy (named criteria); (4) restate queue-dark non-enforcement in the admission table itself; (5) §3.7 one-line ops-cost comparison for consensus rejection. gemini DEGRADED (timeout) — 2nd timeout; per-round partial recorded (r1 gemini success keeps spec-level flag clean).
- integration r4: CONVERGED-CLEAN; conformance re-flag RULED fully answered (disclosure of the existing model, each non-goal maps to bounded/loud/converge-back mechanisms; re-flagging disclosure would invert the gate's purpose). 2 minors: INT4-1 widen §0 exemption row to name all three fault windows (error-arm/partition/replication-lag); INT4-2 qualify §8 ceiling-key line per the authority clause.
- security r4: CONVERGED-CLEAN (all r3 minors verified; delta adds no attack surface).
- scalability r4: CONVERGED-CLEAN (all r3 minors verified; delta all bounded).
- decision-completeness r4: 1 minor (DC4-1: name the status surface for ladder/error-arm watchers — fold episode state into GET /pool/duplicate-reconciler or name a ladder block); COUNTS frontloaded=15 cheap=4 cleared=4 rejected=0; open questions none.
- adversarial r4: 1 MATERIAL + 3 minors; all six r3 folds confirmed genuine.
ADV4-1 MAT [§3.0/§3.2.3.3] §3.0's claim "no auto-close without terminate-time owner-liveness proof" not delivered: re-probe is precondition only of the CARVE-OUT legs; unshielded duplicates close on pre-write probe evidence (ticks-to-minutes stale). → make terminate-time re-probe (5s, fail→veto+defer) a precondition of EVERY duplicate-reconciled/topic-moved closeout termination at the shared chokepoint; extend §5 row beyond the open-commitment case. (Strong option = stated design intent.)
ADV4-2 min: flag count drift — say FOUR flags (§8 DEV_GATED_FEATURES, §3.2.0); declare commitmentCustodyTransfer joins COHERENCE_CRITICAL_FLAGS.
ADV4-3 min: Increment-1 landing list enumerates all THREE migrations (2 skill + gitignore).
ADV4-4 min: §3.0 precision — "(ownership records; pin repairs ride the pin store's own fold, §3.2.3)".

## Round 5 — 2026-07-10 ~21:00 PDT
- Body hash: 6b6133d039910e546b16f32b4375c77eb6f265330d4211a1e58a8098299285f2
- Gate: ran (0 flags) — widened §0 exemption row cleared the Cross-Machine Coherence re-flag
- External r5: codex ok MINOR ISSUES (5 editorial: row-level enforceability annotation; operator-escalation-forever alternative + custody-2b-after-measured-frequency note; explicit provenance threat model (local-disk compromise out of scope, posture parity); promote shadow-beats-static into JWF rule text; sequence-diagram deliverable). gemini DEGRADED (timeout, 3rd).
- Internal final verifier: pending.
Planned disposition if verifier clean: CONVERGED at round 5; codex-r5 quintet folded editorially (enumerated in report; the self-action-backpressure round-9 precedent); dissents recorded: full-context arbiter rows stay always-on (DPOR), custody transfer stays in-cycle as Increment 2b (already escalate-only until then; frequency measured during Increment-2 soak justifies the flip).
