# Session-Pool Build — Decision Log

Running log of judgment calls I (Echo) made autonomously during the build, per Justin's
"if any questions come up, make the call yourself and note them so you can let me know later"
(topic 13481, 2026-05-28). Each entry: the call, why, and how to reverse it if Justin disagrees.

---

### D1 — Open Design Decision #5 confirmed: ≤10-machine single-router v0.1
- **Call:** Took Justin's "go!" (without widening the envelope) as confirmation of the spec's recommended scope — v0.1 ships a single fenced router (RouterShardKey always shard 0), supported envelope ≤ `routerPoolMaxMachines` (10) machines / ≤ `routerMaxThroughputMsgPerSec` (500). Stamped `approved: true` + `approver-signoff: signed` on the spec.
- **Why:** It's the recommended default in the spec; it covers the real near-term target (laptop + mini + phone); multi-shard scaling is pre-specified on the `RouterShardKey` seam so it's not retrofit debt. Justin's "go!" was explicit approval and he delegated remaining calls to me.
- **Reverse:** widen `routerPoolMaxMachines` in config (envelope is a knob); multi-shard is its own future spec.

### D2 — Machine-nickname + Machines-tab requirement folded in WITHOUT full re-convergence
- **Call:** Justin added (topic 13481): a Machines dashboard tab (all machines + hardware props), auto-assigned + user-editable nicknames, and transfer-by-nickname ("move this to <nickname>"), with the nickname-driven mid-conversation swap as the headline test-as-self proof. I amended the approved spec in place (L2 registry gets `hardware` + `nickname` + a Machines tab; L4 command grammar resolves nickname→machineId and adds `move this to <nickname>`; Tier-3 + test-as-self gate updated; build plan: Track B builds registry+nicknames+tab, Tracks E/F the resolution+transfer, Track H the proof). I did NOT re-run the full 8-reviewer /spec-converge for this amendment.
- **Why:** The addition is additive and fully consistent with the already-converged L2/L4/dashboard design — no architectural change, no invariant change. Re-running the entire convergence for a well-scoped additive feature would burn hours against Justin's "complete it" directive. Instead I run a FOCUSED lessons-aware + Dashboard-Standard review when Track B (the dashboard tab) is built — that's where the real review risk is (the tab must follow THE Dashboard Standard).
- **Reverse / flag:** If Justin wants the full convergence re-run on the amendment, it's cheap to launch. Flagged in the spec header as a "Post-approval amendment".

### D3 — Track A core change: monotonic-local self-fence for the router lease
- **Call:** The existing `LeaseCoordinator` judges the holder's self-expiry/self-suspend on the WALL clock (`Date.now()` via `now()`), which an NTP step / VM-pause / sleep / CPU-starvation clock jump can fool (the exact SleepWakeDetector failure class). Track A adds a `monotonicNow()` seam (default `performance.now()`) and moves the holder's self-fence (the "have I confirmed a renewal within TTL?" decision in `renew()` and `holdsLease()`) onto the monotonic clock. Wall-clock stays ONLY for the human-readable `acquiredAt`/`expiresAt` display fields. Authority remains the epoch + push-rejection CAS (unchanged — already correct).
- **Why:** Directly implements spec §L−1 ("a holder's own expiry is judged on its monotonic-local clock, never wall-clock") + the LEASE-SUBSTRATE-ROBUSTNESS fold-in + review issue #2. Defense-in-depth: both the wall-clock expiry check and the monotonic self-fence must pass to hold; either can fence (conservative).
- **Reverse:** the monotonic seam is injectable; tests pin both clocks independently.

### D4 — Migration parity via centralized ConfigDefaults, not a bespoke `migrateSessionPoolConfig()`
- **Call:** The spec (round-1 fix #5) named a `migrateSessionPoolConfig()` method in `PostUpdateMigrator`. The actual codebase has since centralized ALL config-default migration into `ConfigDefaults.SHARED_DEFAULTS` + `applyDefaults()` (existence-checked, recursive, idempotent) — `migrateConfig()` calls it generically. So I added the dark `multiMachine.sessionPool` defaults to `SHARED_DEFAULTS` instead of writing a bespoke method. Same guarantee (existing agents get it on update), cleaner (one source of truth, auto-applied to init + migration).
- **Why:** Following the codebase's evolved mechanism beats forcing the spec's older wording; the centralized path IS the migration-parity standard's implementation here. Added `multiMachine: { sessionPool }` ONLY (never `multiMachine.enabled`) so it's inert.
- **Reverse:** the block is add-missing-only; remove the SHARED_DEFAULTS entry to revert.

### D5 — Track A testing tiers: Tier-1 + migration-parity now; Tier-2/3 HTTP+E2E land with Track B's /pool route
- **Call:** Track A introduces NO new HTTP route or user-facing surface (monotonic self-fence + `isRouter()` are internal; the sessionPool config is dark defaults). Per the Testing Integrity Standard, the Tier-3 "feature is alive" E2E is "the single most important test for any feature WITH API routes" — Track A has none. So Track A ships with strong Tier-1 (monotonic self-fence incl. the backward-wall-jump immunity proof, `holdsLease` mid-tick fence, real-monotonic default wiring integrity, `isRouter`, config dark-defaults) + migration-parity tests (the Tier-2-equivalent for a config/migration change). The first user-facing surface is Track B's `GET /pool` — that's where the Tier-2 HTTP + Tier-3 "feature is alive" E2E land, covering the registry/nickname/Machines-tab that users actually touch.
- **Why:** Honors the standard's intent (E2E gates routes; Track A has none) without a box-ticking meaningless boot test. Flagged so the Track B PR explicitly carries the Tier-2/3 surface.
- **Reverse:** if a Tier-3 boot-and-assert-config test is wanted for Track A specifically, it's cheap to add.

### D6 — One feature-branch PR (#506) accumulating per-track commits, not 8 separate PRs
- **Call:** The spec's build plan says "PR to JKHeadley/main" per track. I'm landing all 8 tracks as commits on ONE branch (`echo/cross-machine-session-pool`) under ONE PR (#506), each commit going through the full pre-commit ship-gate (trace + side-effects artifact + spec tags) + CI on push.
- **Why:** The feature ships DARK as one coherent unit; 8 separate PRs would mean 8× ship-gate ceremony + 8× CI cost + a fragmented review, and a dark feature doesn't release until the whole thing + the real-hardware/test-as-self proof passes. One long-lived PR with per-track commits is the standard shape for a large dark feature; CI validates every push. Merges when Track H's proof is green.
- **Reverse:** if Justin wants per-track PRs, split the branch.

---

### D7 — Autonomous-loop went idle after Track A; root cause + continuation fix
- **Symptom (Justin caught it, ~52m idle):** after Track A landed + the milestone, the session went idle instead of rolling into Track B. Dashboard showed "Ran 5 stop hooks", `iteration` never advanced.
- **Root cause:** this box repeatedly respawns my session under load → session-id rotates (a14b2ea0 → e2a509ed → … → 3d1b3cf1). The autonomous-stop-hook resolves ownership via `MY_TOPIC` (tmux→`state/session-registry.json` topicToSession reverse-lookup). **`state/session-registry.json` is MISSING on this box**, so MY_TOPIC empties → the hook reads the LEGACY single-file state (`.instar/autonomous-state.local.md`, topic 12476, `active:false`) instead of my active `.instar/autonomous/13481.local.md` → sees no active job → allows exit. The session-id backstop also can't match (stale). Net: hook never re-prompted.
- **Second bug (non-blocking noise):** settings.json Stop hook references `.instar/hooks/instar/build-stop-hook.sh` which does NOT exist → errors "No such file or directory" on every Stop (non-blocking, cosmetic). Same path-drift class as the historical autonomous-stop-hook path bug.
- **Continuation fix (what I'm doing):** drive continuation with a self-scheduled wakeup (ScheduleWakeup) I control — immune to session-id churn + the missing registry. Commit each track durably (branch + PR) so any hard restart resumes from this decision log. <!-- tracked: session-pool-autonomy-continuation -->
- **Tracked instar follow-ups (fleet-wide, make after the session-pool build or if they re-block me):**
  1. autonomous-stop-hook: when MY_TOPIC is unresolvable (no session-registry), FALL BACK to scanning `.instar/autonomous/*.local.md` for any `active:true` job and adopt it (bootstrap/reconcile), instead of dropping to the inactive legacy file. <!-- tracked: session-pool-autonomy-continuation -->
  2. settings.json Stop hook references a missing `.instar/hooks/instar/build-stop-hook.sh` → fix the path or stop installing the entry; add a PostUpdateMigrator check that every wired Stop-hook path exists (the historical autonomous-stop-hook path-drift lesson, generalized). <!-- tracked: session-pool-autonomy-continuation -->

### D8 — REAL root cause of the autonomous-loop strand: hook CWD-dependence (D7 was a symptom)
- **D7 was wrong/incomplete.** Re-stranded after I claimed it fixed. Reproduced the ACTUAL cause: the autonomous-stop-hook resolves its state via paths RELATIVE to the process CWD (`.instar/autonomous/<topic>.local.md`, the registry, the legacy file). The Stop hook inherits the session's shell CWD — and I've been building inside the worktree (`.worktrees/session-pool`), where the Bash shell CWD persists. So when the hook fired with CWD=worktree, the relative paths resolved against the worktree (no autonomous state) → "no active job" → exit → strand. It blocked the ONE time because that turn's CWD happened to be the agent home. (Same cause for the `build-stop-hook.sh: No such file` noise — relative path vs worktree CWD.)
- **Fix (verified, reproduced both ways):** anchor the hook to `CLAUDE_PROJECT_DIR` (Claude Code always sets it for hooks = the agent home; the hook is itself wired as `${CLAUDE_PROJECT_DIR}/.claude/...`) before resolving relative paths — `if CLAUDE_PROJECT_DIR has .instar → cd there`. Test-safe: when unset (isolated test harness) it stays in CWD, so the existing 40 hook tests pass unchanged. Verified: ran the hook from the worktree CWD with CLAUDE_PROJECT_DIR=agent-home → `decision: block` (was: exit/strand). Applied to BOTH my live agent-home hook (loop works now) and the repo bundled source (new agents via `ensureAutonomousStopHook` deploy). Regression test: tests/unit/autonomous-stop-hook-cwd-anchor.test.ts (3, green).
- **Tracked follow-ups (gated — need src/PostUpdateMigrator + a spec):** <!-- tracked: session-pool-autonomy-continuation -->
  1. Existing-agent delivery: extend `ensureAutonomousStopHook` force-update to re-copy the hook when the deployed copy LACKS the CLAUDE_PROJECT_DIR anchor (content-sniff), so existing agents get the fix on update (new agents already covered via bundled source).
  2. build-stop-hook.sh: re-wire its settings.json Stop entry to `${CLAUDE_PROJECT_DIR}/...` (it's currently a bare relative path → same worktree-CWD failure, non-blocking).

### D9 — Track B agent-awareness: Tier-0 CLAUDE.md blurb now; code-enforced Playbook deep-trigger tracked
- **Call:** Delivered the Agent Awareness Standard's core for Track B — a Tier-0 blurb in `generateClaudeMd()` (Multi-Machine Session Pool: Machines tab, `GET /pool`, `PATCH /pool/machines/:id`, the "run this on / move this to <nickname>" proactive triggers, "where is this running?") + a `migrateClaudeMd()` content-sniff append so existing agents get it. Migration-parity test green.
- **Why softened the Playbook clause:** the spec's Agent Awareness also specifies a CODE-ENFORCED Playbook deep-entry (`multiMachine-placement-deep` with a trigger regex + a SelfKnowledgeTree probe — Structure>Willpower for on-demand deep mechanics). That's a larger build (playbook-manifest seed migration + probe + tests). Rather than assert a non-existent Playbook entry in the blurb (dishonest), I pointed "deep mechanics" at the Machines tab + the spec, and TRACK the code-enforced Playbook trigger as the spec-specified enhancement. The core Standard (the agent KNOWS the feature + endpoints + triggers) is met.
- **Tracked follow-up:** seed `multiMachine-placement-deep` into the Playbook manifest (PostUpdateMigrator playbook-seed migration, like migrateConversationalCatalogPlaybookManifest) with the §L2 trigger regex + the SelfKnowledgeTree probe; then restore the Playbook reference in the blurb. <!-- tracked: session-pool-track-b -->
- **Track B is now functionally complete:** B(1) nicknames, B(2a) registry+clock-FSM, B(2b) /pool API+hardware, B(2c) server wiring + Machines dashboard tab, agent-awareness blurb + migration parity. Remaining Track-B refinement = the Playbook deep-trigger (above). Next: Track C (MeshRpc L0).

## Track A — STATUS: built + pushed + PR #506 (CI green-trending)
Commits: b159cc0d7 (code), be95e86f3 (spec deferral markers). Local: 155 green + tsc clean. CI on #506: fast gates pass, unit shards passing.

## Track B — groundwork (foundation map, so the next turn doesn't re-explore)
Goal: Machine-Pool Registry (L2) + Machines dashboard tab (hardware + editable nicknames) + clock-skew quarantine FSM + `GET /pool` + `PATCH /pool/machines/:id`. Nickname→machineId resolver (consumed by L4).
- **No existing `nickname` field.** `MachineRegistryEntry` (src/core/types.ts ~1497-1542) has `name` (static, set by `detectMachineName()` at creation, MachineIdentity.ts ~206). ADD editable `nickname` to `MachineRegistryEntry`; auto-assign via a new `NicknameAssigner` pure helper (derive from hostname → platform-arch → numeric-suffix collision).
- **MachineIdentityManager** (src/core/MachineIdentity.ts): registry at `.instar/machines/registry.json`; methods registerMachine(302)/updateRole(320)/touchMachine(333)/updateMachineUrl(345)/getActiveMachines(411)/getAwakeMachine(398). Add `updateNickname(machineId, nickname)` + nickname-uniqueness validation + a `resolveNickname(name)→machineId` lookup.
- **MachineHeartbeat** (src/core/MachineHeartbeat.ts): HeartbeatRecord {machineId, hostname, lastHeartbeatAt, instarVersion}; store `.instar/machine-health/<id>.json`; interval 30min, stale 48h. Reuse for liveness.
- **MachineCapacity** (NEW, per spec §L2): {machineId, nickname, online, selfReportedLastSeen, routerReceivedAt, loadAvg, memPressure, activeSessionCount, maxSessions, capabilities[], modelsAvailable[], agentsResident[], hardware, clockSkewStatus}. hardware via `os` (totalmem/cpus()[0].model/cpus().length/platform/arch/hostname). loadAvg via `os.loadavg()`. mem via SessionManager.getDiagnostics() (SessionDiagnostics: maxSessions, memoryPressure, memoryUsedPercent, freeMemoryMB, totalMemoryMB).
- **clockSkewStatus FSM** (spec §L2): enum {ok, divergence-detected-once, suspect-clock-removed} + removed-clean-count; transitions per the spec table (2-divergent→removed+Attention, 2-clean→re-admit). Pure helper + tests both sides.
- **Routes** (src/server/routes.ts): NO existing /pool or /machines. Bearer-auth pattern: `req.headers.authorization` startsWith 'Bearer ' === ctx.config.authToken else 401. Add `GET /pool` (router holder + machine capacities + session→machine map) + `PATCH /pool/machines/:id {nickname}`. getSyncStatus() (MMCoordinator ~421) returns {enabled,role,leaseHolder,leaseEpoch,holdsLease,splitBrainState,protocolVersion,awakeMachineCount} — extend or parallel /pool.
- **Dashboard**: single `dashboard/index.html`, embedded JS, tab pattern: button `<button class="tab" data-tab="machines" onclick="switchTab('machines')">`, a `.tab-content` `id="machinesTab"`, register in `TAB_REGISTRY`, CSS in `<style>`. Existing tabs incl. secrets/threadline/process-health/initiatives. Follow THE Dashboard Standard ([[feedback_dashboard_copy_eli16]]): ELI16 copy, fixed status-dot vocab, expandable items, no jargon. Tab tests pattern: tests/unit/dashboard-secretsTab.test.ts, dashboard-initiativesTab.test.ts.
- **Tests**: tests/unit/MachineHeartbeat.test.ts exists. Add: NicknameAssigner (deterministic + collision), clockSkewStatus FSM (both sides), /pool + PATCH routes (integration), Machines tab (dashboard unit), nickname resolver. Tier-3 "feature alive": GET /pool returns 200 with machine list on a booted server (the first user-facing surface → the Track-B E2E that D5 deferred from Track A).
- **Migration parity**: extend the `multiMachine.sessionPool` ConfigDefaults block with any Track-B knobs (clockSkewToleranceMs, maxExpectedNtpDriftMs, machineRecordEvictionMs) via the same applyDefaults path. CLAUDE.md Tier-0 blurb + Playbook seed land here (first user-facing surface) per spec Agent Awareness.

## D11 — Track E (Session Router / Placement Engine, L4): the dark-ship activation boundary
- **Built (3 sub-commits, all 3 test tiers, dark):**
  - **E(1)** `PlacementExecutor.decide()` (pure, data-driven `PlacementPolicy`, schema-validated at construction; hard-constraint→pin→sticky→least-loaded; queue+escalate on unsatisfiable pin/capability — never silent mis-place; `validateTopicPlacement()` blocks corrupt metadata) + `recognizeNicknameCommand()` (conservative deterministic "move/run this on <nick>" recognizer; verb+known-nickname required; longest-match; pin vs transfer). Tier-1: 15 + 11.
  - **E(2)** `SessionRouter.route()` — the §L4 dispatch FSM (local / deliverMessage-forward / owner-dead-replace / transient-queue / unowned-place→synchronous-CAS-claim→spawn; per-session in-order one-in-flight; injected I/O). + `MeshRpc.deliverMessage` command (router-only RBAC, §L0 signed envelope). Tier-1: 15 router; Tier-2: 3 over REAL MeshRpc + SessionOwnershipRegistry (forward, idempotent redelivery, two-router CAS exactly-one-owner).
  - **E(3)** boot wiring: shared `createDeliverMessageHandler` factory (imported by BOTH server.ts boot AND tests — Structure>Willpower, no drift) wired into the boot mesh dispatcher (owner-side: stale-ownership fence + idempotent durable-receipt via the real MessageProcessingLedger). Tier-1: 5; Tier-3 "feature alive" E2E: 4 over the production handler + real /mesh/rpc + real SQLite ledger + real registry (queued→duplicate→stale-ownership→non-router-403). Config tunables (`deliverMessage*`, `placementHysteresisDelta`, `ownershipCasMaxRetries`) added to the dark `sessionPool` block + SessionPoolConfig type + a migration-parity backfill test (add-missing into a partial block).
- **The activation boundary (NOT a deferral — the designed dark-ship rollout):** Track E ships the dispatch BRAIN fully built + 3-tier tested. Two pieces are deliberately Track-H, gated by `StageAdvancer`, because they only become correct/provable WITH real peers + hardware:
  1. **Live-ingress interception** ("consult placement instead of always spawning locally", spec line 572) — flipping the actual Telegram ingress to route through `SessionRouter`. This is the staged behavior the whole `stage: dark→shadow→live-transfer` ladder exists to gate. Activating it now (no stage ladder, no real peers) would be the unsafe thing.
  2. **Outbound mesh client** (the router's `deliverMessage`/`spawnOnMachine` deps over the wire to peer machines) — needs real peer addressing/URLs, which exist in the Track-H two-machine bring-up. The RECEIVE side is fully wired now (so an owner can accept forwards the moment a sender exists).
- **Agent-awareness:** already delivered in Track B's Tier-0 blurb (template + migrator) — it already documents the "run this on / move this to <nickname>" triggers; Track E's recognizer makes that documented trigger actually fire. No new CLAUDE.md change needed.
- **Track E is functionally complete.** Next: Track F (Transfer/Handoff Orchestrator + transfer-by-nickname, L5) — which builds on E's recognizer (pin/transfer intent) + the SessionOwnership transfer FSM (active→transferring→active).

## D12 — Track F (Transfer/Handoff Orchestrator, L5): built dark, 3 tiers
- **Built (2 sub-commits):**
  - **F(1)** pure cores: `planTransferByNickname()` (headline "move this to <nick>" planner — resolve/reject-unknown/rate-limit/no-op/confirm-gate) + `TransferOrchestrator.transfer()` (the §L3/§L5 ordered handoff active(S)→transferring→active(T)→S-release; drain bound + output-exclusion timing; abort paths) + `verifyLedgerSnapshot()` (SHA + all-terminal). Tier-1: 8 + 10.
  - **F(2)** Tier-2 integration over the REAL SessionOwnershipRegistry transfer FSM + REAL verifyLedgerSnapshot (clean S→T transfer reaches active(T)@epoch4; in_flight snapshot → sync-corrupted leaves the record at transferring(S) — no no-owner gap, T never claims). + §L5 config tunables (transferDrainTimeoutMs/outputCutoffMs/placementCooldownMs/topicPlacementUpdateMinIntervalMs) with add-missing migration-parity backfill test.
- **Reused the existing 'transfer' MeshRpc command + ownership FSM** (`transfer`/`claim` actions already in SessionOwnership.ts from Track D) — the orchestrator drives them; no new mesh command needed for the handoff CAS sequence. Source "release" is LOCAL teardown (the registry already shows active(T) after the target claims), NOT a registry release CAS — matches spec line 343 ("release is a cleanup step, not a precondition").
- **Activation boundary (Track-H, D11):** real drain (SessionMigrator), real git ledger flush (flushToGit), the SyncManifest atomic-sync pull, and the outbound mesh client carrying ledgerSnapshotRef to a real peer are the hardware-proven activation. The orchestrator + planner + verify are fully built + 3-tier tested against the real FSM now.
- **Agent-awareness:** Track B's blurb already documents "move this to <nickname>" → transfer (resumes like a session restart). Track F makes it fire. No new CLAUDE.md change.
- **Track F functionally complete.** Next: Track G — Multi-Agent-Per-Machine (L6).

## D13 — Track H: all CODE built (dark + tested); the ACTIVATION is a staged operation, not more code
- **Built dark + tested this session (Track H):** StageAdvancer + signed SessionPoolE2EResultStore (14) + stage-write guard (6) + boot wiring + GET /session-pool/e2e-results (2) + RebalancePlanner (8) + outbound MeshRpcClient (4 round-trip). The m2m transport is now COMPLETE (send `MeshRpcClient` + receive `MeshRpcDispatcher`, round-trip-proven). Every architectural layer L0–L6 + the rollout gate is built behind the dark flag.
- **What remains is an OPERATION, not buildable code:** the headline "move this to <nickname>" proof needs (a) the live-ingress interception — flipping THIS agent's Telegram dispatch to route through the SessionRouter (inert while dark; the gated branch); (b) wiring MeshRpcClient as the router's outbound deps; (c) the mini brought up as a 2nd pool machine; (d) advancing the stage ladder dark→shadow→live-transfer, each gated by a real green E2E (now enforced by StageAdvancer); (e) test-as-self Playwright driving the swap + verifying exactly-once continuation.
- **Why this is deliberately the last, careful step (NOT a lazy deferral):** the live-ingress flip changes how every message I handle is dispatched — a mistake breaks my own messaging with Justin mid-run. The spec's staged rollout (dark→shadow→live-transfer→rebalance, each E2E-gated) exists precisely to de-risk this; rushing the live-dispatch flip on the prod laptop in an autonomous window is the one thing the whole rollout architecture is designed to avoid. The honest sequence is mini-first, shadow before transfer, E2E-recorded at each gate. "Say go once / complete delivery" is satisfied on the CODE (all built); the activation is a controlled hardware operation.
- **Activation entry point (for the next phase):** construct SessionRouter in boot with deps = { resolveOwnership/casClaimOwnership ← sessionOwnershipRegistry; placement ← PlacementExecutor(policy from config); machineRegistry ← MachinePoolRegistry.getCapacities; deliverMessage/spawnOnMachine ← MeshRpcClient.send to the owner peer; handleLocally ← the existing local spawn/resume path; isMachineAlive ← registry liveness }. Gate the ingress interception on `sessionPool.enabled && stage !== 'dark'` so dark = today's always-local behavior, byte-identical.
